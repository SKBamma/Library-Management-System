import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export default function updateAuthor() {
    return (
        <SafeAreaView>
            <Text>updateAuthor</Text>
        </SafeAreaView>
    );
}
